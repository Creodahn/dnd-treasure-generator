<Ui::LabeledInput
  @disabled={{true}}
  @input-id="email-input"
  @label="Email"
  @value={{this.profile.email}}

  @update={{fn (mut this.profile.email)}}
/>

<Ui::LabeledInput
  @disabled={{true}}
  @input-id="name-input"
  @label="Full Name"
  @value={{this.profile.name}}

  @update={{fn (mut this.profile.name)}}
/>