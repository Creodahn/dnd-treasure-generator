<nav class="border split-nav">
  <div class="nav-brand">
    <h3>
      {{#link-to "main.home"}}
        D&amp;D Randomizer
      {{/link-to}}
    </h3>
  </div>
  <div class="collapsible">
    {{input
      checked=collapsibleOpen
      id="collapsible1"
      name="collapsible1"
      type="checkbox"
    }}
    <button {{action "toggleCollapsible"}}>
      <div class="bar1"></div>
      <div class="bar2"></div>
      <div class="bar3"></div>
    </button>
    <div class="collapsible-body">
      <ul class="inline">
        <li>
          {{#link-to "main.treasure"}}
            Generate Treasure
          {{/link-to}}
        </li>
        <li>
          {{#link-to "main.dice-roll"}}
            Roll Dice
          {{/link-to}}
        </li>
        <li>
          {{#link-to "main.reference"}}
            Reference
          {{/link-to}}
        </li>
      </ul>
    </div>
  </div>
</nav>
{{outlet}}